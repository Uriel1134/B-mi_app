import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF2F9359);  // Vert principal
  static const Color backgroundColor = Color(0xFFEBF4E3);  // Fond légèrement verdâtre
  static const Color textColor = Color(0xFF1A1A1A);  // Texte principal
  static const Color secondaryColor = Color(0xFF2196F3);  // Bleu pour les accents
  static const Color accentColor = Color(0xFF8BC34A);  // Vert clair pour les accents
} 